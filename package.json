{
  "engines": {
    "node": ">=5.11.1"
  }
  "scripts": {
    "postinstall": "cd web && npm install && bower install && grunt build"
  },
  "dependencies": {
    "grunt-cli": "^1.2.0",
    "bower": "1.7.9"
  }
}
